{
  "name": "sfrt-sphere-lattice-web",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based SFRT sphere lattice tool (CT/RTSTRUCT → derived RTSTRUCT) (research use only).",
  "license": "LicenseRef-PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "build:standalone": "node scripts/build-standalone.mjs",
    "build:tauri": "node scripts/build-tauri.mjs",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run build:tauri && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
